project_id  = "my-gcp-project"
bucket_name = "my-gcp-project-terraform-state"
location    = "US"

labels = {
  managed_by = "terraform"
  purpose    = "terraform-state"
  repository = "terraform-gcp-dotnet-blueprint"
}
