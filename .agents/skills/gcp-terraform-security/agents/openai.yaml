interface:
  display_name: "GCP Terraform Security"
  short_description: "Harden Terraform state, IAM, secrets, and WIF"
  default_prompt: "Use the GCP Terraform security skill to review state, IAM, secrets, public access, and GitHub-to-GCP Workload Identity Federation before implementing the change."
